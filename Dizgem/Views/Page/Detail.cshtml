@model Page

@{
    ViewData["Title"] = Model.Title;
}

<h1>@Model.Title</h1>
@{
    if (Model.CoverPhotoMediaId != Guid.Empty && Model.CoverPhoto != null)
    {
        <img class="img img-fluid img-shadow" src="@Model?.CoverPhoto.GetSize("full")" />
    }
}

@Model.RenderedContent
